# Hệ thống Báo cháy 2 Camera – Python dependencies
# Cài đặt: pip install -r requirements.txt

ultralytics>=8.0.0          # YOLOv8 (kèm PyTorch)
opencv-python>=4.8.0        # Đọc camera, xử lý ảnh, hiển thị
pyserial>=3.5                # Giao tiếp Serial với ESP32
numpy>=1.24.0                 # Tính toán mảng / mask pixel
colorama>=0.4.6               # Màu sắc cảnh báo trên terminal (Windows/VS Code)
